# ============================================================
# EXAMPLE ENVIRONMENT FILE — copy to .env.local for local dev
# ============================================================
# DO NOT commit real keys — this file is safe to commit.
# ============================================================

# ── Backend API URL ──────────────────────────────────────────
# Production (Render):
VITE_API_URL=https://nexsus-ai.onrender.com
# Local development (uncomment and use in .env.local instead):
# VITE_API_URL=http://localhost:5000

# ── Gemini AI (client-side SDK) ──────────────────────────────
VITE_GEMINI_API_KEY=your_google_gemini_api_key
